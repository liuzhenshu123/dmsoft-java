package cn.com.qjun.dmsoft.utils;

import com.sun.jna.ptr.IntByReference;

public class WindowsUtils {
    static {
//        System.loadLibrary("mydll"); // 加载包含GetWindowThreadProcessId实现的动态链接库
//        System.loadLibrary("windowsutil"); // 加载包含本地方法实现的动态链接库
    }

//    public native int getWindowThreadProcessId(long hwnd, IntByReference processId);
//    public native long findWindowByTitle(String title);
}
